// import { PackageSettings } from 'arui-scripts';

const settings = {
    modules: {
        shared: {
            'react': '^19.1.0',
            'react-dom': '^19.1.0',
        },
    }
};

export default settings;
